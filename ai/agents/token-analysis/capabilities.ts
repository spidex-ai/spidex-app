export const TOKEN_ANALYSIS_AGENT_CAPABILITIES =
`The token analysis bot gets trading data for a given token.

It can get the top holders, the price chart, the number of holders, the current price and volume, the bubble map (which shows token distribution), and the top traders of a token.

Call this agent whenever the user asks for data about a specific token.`;